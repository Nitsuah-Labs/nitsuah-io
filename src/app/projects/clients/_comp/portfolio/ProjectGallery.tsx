/**
 * ProjectGallery.tsx
 *
 * TODO: Extract from PortfolioDemo.tsx (745 LOC)
 *
 * This will handle the project gallery view showing:
 * - Project grid with filtering by category (Branding, Photography, Design)
 * - Project cards with hover effects
 * - Project detail modal/view
 * - Category badges and icons
 * - Responsive grid layout
 *
 * Target: ~300 LOC
 *
 * Can use shared components:
 * - DemoCard for project cards
 * - DemoButton for category filters
 * - DemoSection for project details
 *
 * Lines to extract from PortfolioDemo: ~172-438
 */

"use client";
import React from "react";

export const ProjectGallery: React.FC = () => {
  return (
    <div>
      <p style={{ color: "#888", textAlign: "center", padding: "2rem" }}>
        Project Gallery - To be extracted from PortfolioDemo
      </p>
    </div>
  );
};
