#!/bin/sh
# Add Node.js to PATH for Git Bash on Windows
export PATH="/c/Program Files/nodejs:$PATH"

echo "⚡ Pre-push: Fast-forwarding to CI (no local checks)"
echo "✅ Push accepted. CI will run full validation (build, tests, accessibility, visual regression)."
