#!/bin/sh
# Add Node.js to PATH for Git Bash on Windows
export PATH="/c/Program Files/nodejs:$PATH"

echo "🔍 Running pre-push validation (quick smoke test)..."

# Fast lint check catches most syntax/style issues
# Note: Skipping next lint due to Next.js 16 bug - CI will validate with build
echo "1/2 Skipping lint (Next.js 16 bug - CI validates in build)..."

# Quick typecheck catches type errors before CI
echo "2/2 Type checking..."
npm run typecheck || exit 1

echo "✅ Pre-push checks passed! (~5-10s) CI will run full build + tests."
